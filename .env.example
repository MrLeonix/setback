# Copy to .env and adjust for local overrides. Never commit the real .env.
# See src/setback/config.py for the defaults these override.

SETBACK_GCP_PROJECT=setback-app
SETBACK_GCS_BUCKET=setback-app-corpus
